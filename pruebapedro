local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ManitoGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

---------------------------------------
-- FUNCION PARA HACER CUALQUIER FRAME DRAGGABLE
---------------------------------------
local function makeDraggable(frame)
    local dragging = false
    local dragStart, startPos

    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    frame.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
                                       startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

--------------------------------------------------
-- VENTANA PRINCIPAL
--------------------------------------------------
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 300, 0, 200)
Main.Position = UDim2.new(0.3, 0, 0.3, 0)
Main.BackgroundColor3 = Color3.fromRGB(32, 32, 32)
Main.Active = true
Main.Draggable = false
Main.Parent = ScreenGui
makeDraggable(Main)

--------------------------------------------------
-- BOTON CERRAR
--------------------------------------------------
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 25, 0, 25)
CloseButton.Position = UDim2.new(1, -30, 0, 5)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.new(1, 1, 1)
CloseButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
CloseButton.Parent = Main

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

--------------------------------------------------
-- BOTON MINIMIZAR
--------------------------------------------------
local Minimize = Instance.new("TextButton")
Minimize.Size = UDim2.new(0, 25, 0, 25)
Minimize.Position = UDim2.new(1, -60, 0, 5)
Minimize.Text = "-"
Minimize.TextColor3 = Color3.new(1, 1, 1)
Minimize.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
Minimize.Parent = Main

--------------------------------------------------
-- CÍRCULO FLOTANTE CUANDO ESTÁ MINIMIZADO
--------------------------------------------------
local Circle = Instance.new("Frame")
Circle.Size = UDim2.new(0, 60, 0, 60)
Circle.BackgroundColor3 = Color3.fromRGB(50, 120, 255)
Circle.Visible = false
Circle.Parent = ScreenGui
makeDraggable(Circle)

local UICorner = Instance.new("UICorner", Circle)
UICorner.CornerRadius = UDim.new(1, 0)

Circle.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        Main.Visible = true
        Circle.Visible = false
    end
end)

Minimize.MouseButton1Click:Connect(function()
    Main.Visible = false
    Circle.Visible = true
end)

--------------------------------------------------
-- OPCIONES Y TOGGLES
--------------------------------------------------
local player = game.Players.LocalPlayer
local humanoid = player.Character:WaitForChild("Humanoid")

-- Crear un layout
local Layout = Instance.new("UIListLayout")
Layout.Parent = Main
Layout.Padding = UDim.new(0, 8)
Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
Layout.VerticalAlignment = Enum.VerticalAlignment.Top
Main.Padding = 10

local function createToggle(name)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 30)
    frame.BackgroundTransparency = 1
    frame.Parent = Main

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.TextColor3 = Color3.new(1, 1, 1)
    label.Text = name
    label.Parent = frame

    local checkbox = Instance.new("TextButton")
    checkbox.Size = UDim2.new(0.2, 0, 1, 0)
    checkbox.Position = UDim2.new(0.75, 0, 0, 0)
    checkbox.Text = "OFF"
    checkbox.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
    checkbox.TextColor3 = Color3.new(1, 1, 1)
    checkbox.Parent = frame

    return checkbox
end

--------------------------------------------------
-- TOGGLES
--------------------------------------------------
local speed100 = createToggle("Velocidad 100")
local speed300 = createToggle("Supervelocidad 300")
local jump = createToggle("Salto alto")
local fuerza = createToggle("Fuerza extra")
local nocool = createToggle("Quitar cooldown (desde tu script)")

--------------------------------------------------
-- FUNCIONES
--------------------------------------------------
speed100.MouseButton1Click:Connect(function()
    if speed100.Text == "OFF" then
        speed100.Text = "ON"
        speed100.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        humanoid.WalkSpeed = 100
    else
        speed100.Text = "OFF"
        speed100.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
        humanoid.WalkSpeed = 16
    end
end)

speed300.MouseButton1Click:Connect(function()
    if speed300.Text == "OFF" then
        speed300.Text = "ON"
        speed300.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        humanoid.WalkSpeed = 300
    else
        speed300.Text = "OFF"
        speed300.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
        humanoid.WalkSpeed = 16
    end
end)

jump.MouseButton1Click:Connect(function()
    if jump.Text == "OFF" then
        jump.Text = "ON"
        jump.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
        humanoid.JumpPower = 75
    else
        jump.Text = "OFF"
        jump.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
        humanoid.JumpPower = 50
    end
end)

fuerza.MouseButton1Click:Connect(function()
    print("⚠️ Aquí debes poner tu propia lógica de fuerza")
end)

nocool.MouseButton1Click:Connect(function()
    print("⚠️ Aquí conectas tu sistema para remover cooldowns en TU juego.")
end)
